server {
    listen 80;
    listen [::]:80;
    
    server_name fieldengineerpro.com www.fieldengineerpro.com;
    root /var/www/fieldops-pro/public;
    
    index index.php index.html;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Logging
    access_log /var/log/nginx/fieldops-access.log;
    error_log /var/log/nginx/fieldops-error.log;
    
    # Client uploads
    client_max_body_size 100M;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
    
    # Laravel application
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    # PHP-FPM configuration
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
        
        # Increase timeouts for long-running requests
        fastcgi_read_timeout 300;
        fastcgi_send_timeout 300;
    }
    
    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }
    
    # Deny access to sensitive files
    location ~ /\.(?!well-known).* {
        deny all;
    }
    
    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# SSL configuration (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     
#     server_name fieldengineerpro.com www.fieldengineerpro.com;
#     root /var/www/fieldops-pro/public;
#     
#     # SSL certificates (managed by Certbot)
#     ssl_certificate /etc/letsencrypt/live/fieldengineerpro.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/fieldengineerpro.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     
#     # Same configuration as above...
#     index index.php index.html;
#     client_max_body_size 100M;
#     
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#     
#     location ~ \.php$ {
#         fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         include fastcgi_params;
#         fastcgi_hide_header X-Powered-By;
#         fastcgi_read_timeout 300;
#     }
#     
#     location ~ /\. {
#         deny all;
#     }
#     
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }

# Redirect HTTP to HTTPS (uncomment after SSL is set up)
# server {
#     listen 80;
#     listen [::]:80;
#     server_name fieldengineerpro.com www.fieldengineerpro.com;
#     return 301 https://$server_name$request_uri;
# }
